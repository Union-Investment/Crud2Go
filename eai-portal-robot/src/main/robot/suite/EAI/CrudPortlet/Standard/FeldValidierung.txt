*** Settings ***
Documentation   Story: Als EAI-Verantwortlicher möchte ich Validierungsregeln konfigurieren können

*** Test Case ***
Validierung schlägt fehl
	[Tags]  EAI_74
	
	Es existiert ein Portlet mit der XML-Konfiguration 'VALIDIERUNG_REQUIRED.xml'
	
	und dann eine neue Zeile einfügt und den Editieren Modus verlässt
	dann wird die Validierungsfehlermeldung 'NAME: Bitte geben Sie einen Wert ein!' angezeigt


Maximale Länge schlägt fehl
	[Tags]  EAI_74
	
	Es existiert ein Portlet mit der XML-Konfiguration 'VALIDIERUNG_MAXLEN.xml'
	
	und dann eine neue Zeile mit dem Wert '1234567890-' einfügt
	und dann wird die Spalte '1234567890' angezeigt
	
	
Regulärer Ausdruck ([0-9]+) schlägt fehl
	[Tags]  EAI_74
	
	Es existiert ein Portlet mit der XML-Konfiguration 'VALIDIERUNG_REGEXP.xml'
	
	und dann eine neue Zeile mit dem Wert '1A' einfügt
	dann wird die Validierungsfehlermeldung 'NAME: Bitte geben Sie nur Zahlen ein!' angezeigt


Escape-ter regulärer Ausdruck ([0-9]+,[0-9]+) schlägt fehl
	[Tags]  EAI_74
	
	Es existiert ein Portlet mit der XML-Konfiguration 'VALIDIERUNG_REGEXP_ESCAPE.xml'
	
	und dann eine neue Zeile mit dem Wert '1,00A' einfügt
	dann wird die Validierungsfehlermeldung 'NAME: Bitte geben Sie nur Zahlen (mit Komma) ein!' angezeigt


Escape-ter regulärer Ausdruck ([0-9]+,[0-9]+) klappt
	[Tags]  EAI_74
	
	Es existiert ein Portlet mit der XML-Konfiguration 'VALIDIERUNG_REGEXP_ESCAPE.xml'
	
	und dann eine neue Zeile mit dem Wert '1,0' einfügt
	und dann wird die Spalte '1,0' angezeigt
	
	
Mehrere Regeln (required,regexp=[0-9]+) - eine schlägt fehl
	[Tags]  EAI_74
	
	Es existiert ein Portlet mit der XML-Konfiguration 'VALIDIERUNG_MULTIPLE.xml'
	
	und dann eine neue Zeile mit dem Wert '10-' einfügt
	dann wird die Validierungsfehlermeldung 'NAME: Der Eingabe ist ungueltig!' angezeigt


Mehrere Regeln (required,regexp=[0-9]+) - alle klappen
	[Tags]  EAI_74
	
	Es existiert ein Portlet mit der XML-Konfiguration 'VALIDIERUNG_MULTIPLE.xml'
	
	und dann eine neue Zeile mit dem Wert '12345' einfügt
	und dann wird die Spalte '12345' angezeigt	


InputPromt Anzeige
	[Tags]  EAI_74  
	
	Es existiert ein Portlet mit der XML-Konfiguration 'INPUT_PROMT.xml'
	
	und dann eine neue Zeile einfügen aufruft	
	
	dann wird der InputPromt 'Name eintragen' in der Spalte '2' angezeigt
	dann wird der InputPromt 'Wert eintragen' in der Spalte '3' angezeigt

*** Settings ***
Force Tags      autor-mca
...				EAI_74
Resource        ${RESOURCES}/import/CrudPortlet.txt
